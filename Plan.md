# README Improvement Plan

- Add a "Quick Start" section near the top with a minimal end-to-end example, ahead of the full installation instructions, so new users can validate their setup in under a minute.
- Reorganize the examples list into collapsible sections (Configuration, Basics, Streaming, Advanced) using `<details>` blocks to reduce initial scroll length while keeping content discoverable.
- Refresh the compatibility matrix to include the currently supported Kubernetes versions and link each row to the corresponding release notes.
- Add a "Troubleshooting / FAQ" section covering common setup issues (auth, TLS, proxy configs) gathered from existing GitHub issues.
- Include badges for build status, code coverage, and latest release version at the top for at-a-glance project health.
